{
  "name": "family-cost-planner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate:data": "node scripts/validate-data.mjs data/costs.v1.json",
    "validate:candidate": "node scripts/validate-data.mjs data/candidate.json --baseline data/costs.v1.json"
  },
  "devDependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1"
  }
}
